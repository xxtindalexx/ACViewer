<UserControl x:Class="ACViewer.View.FileExplorer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ACViewer.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="22.5"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition MaxWidth="178"/>
        </Grid.ColumnDefinitions>
        <Label Content="File Type" Grid.ColumnSpan="2"/>
        <ComboBox Name="FileType" Width="174" ItemsSource="{Binding FileTypes}" Grid.Row="1"  Grid.ColumnSpan="2" Margin="7.5,0,0,0" SelectionChanged="FileType_SelectionChanged" HorizontalAlignment="Left"/>

        <!-- Left column: Files and FileInfo with independent splitter -->
        <Grid Grid.Row="2" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" MinHeight="100"/>
                <RowDefinition Height="4"/>
                <RowDefinition Height="*" MinHeight="100"/>
            </Grid.RowDefinitions>
            <ListBox Name="Files" Margin="7.5,2,0,0" ItemsSource="{Binding FileIDs}" Grid.Row="0" SelectionChanged="Files_SelectionChanged"/>
            <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ResizeDirection="Rows" Background="#FFE0E0E0"/>
            <local:FileInfo Grid.Row="2" Margin="7.5,0,0,0" />
        </Grid>

        <!-- Right column: History, MotionList with independent splitter -->
        <Grid Grid.Row="2" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="150" MinHeight="100"/>
                <RowDefinition Height="4"/>
                <RowDefinition Height="*" MinHeight="100"/>
            </Grid.RowDefinitions>
            <local:HistoryPanel Grid.Row="0" Margin="7.5,2,7.5,0" />
            <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ResizeDirection="Rows" Background="#FFE0E0E0"/>
            <local:MotionList Grid.Row="2" Margin="7.5,0,7.5,0" />
            <local:ScriptList Grid.Row="2" Margin="7.5,0,7.5,0" Visibility="Hidden" />
            <local:ClothingTableList Grid.Row="2" Margin="7.5,0,7.5,0" Visibility="Hidden" />
        </Grid>
    </Grid>
</UserControl>
