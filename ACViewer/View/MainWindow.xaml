<Window x:Class="ACViewer.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ACViewer.View"
        xmlns:scenes="clr-namespace:ACViewer"
        xmlns:avalonDock="https://github.com/Dirkster99/AvalonDock"
        d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="ACViewer" Width="1342" Height="798" Style="{DynamicResource CustomWindowStyle}" SourceInitialized="Window_SourceInitialized" Closing="Window_Closing">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <local:MainMenu x:Name="menuMain" Grid.Row="0"/>

        <avalonDock:DockingManager x:Name="MainDockManager" Grid.Row="1" AllowMixedOrientation="True">
            <avalonDock:DockingManager.Layout>
                <avalonDock:LayoutRoot>
                    <avalonDock:LayoutPanel Orientation="Horizontal">

                        <!-- Left side: File Explorer and FileInfo -->
                        <avalonDock:LayoutAnchorablePaneGroup DockWidth="360" Orientation="Vertical">
                            <avalonDock:LayoutAnchorablePane>
                                <avalonDock:LayoutAnchorable x:Name="FileExplorerAnchorable" ContentId="FileExplorer" Title="File Explorer" CanClose="False" CanHide="True">
                                    <local:FileExplorer />
                                </avalonDock:LayoutAnchorable>
                            </avalonDock:LayoutAnchorablePane>
                        </avalonDock:LayoutAnchorablePaneGroup>

                        <!-- Center: GameView as anchorable panel -->
                        <avalonDock:LayoutAnchorablePaneGroup DockWidth="*">
                            <avalonDock:LayoutAnchorablePane>
                                <avalonDock:LayoutAnchorable x:Name="GameViewAnchorable" ContentId="GameView" Title="Viewer" CanClose="False" CanHide="True" CanFloat="True">
                                    <scenes:GameView x:Name="Scene" Focusable="True" PreviewMouseDown="Scene_PreviewMouseDown" PreviewMouseRightButtonDown="Scene_PreviewMouseRightButtonDown" PreviewMouseRightButtonUp="Scene_PreviewMouseRightButtonUp" ContextMenu="{x:Null}" />
                                </avalonDock:LayoutAnchorable>
                            </avalonDock:LayoutAnchorablePane>
                        </avalonDock:LayoutAnchorablePaneGroup>

                    </avalonDock:LayoutPanel>

                    <!-- Bottom: Status panel -->
                    <avalonDock:LayoutRoot.BottomSide>
                        <avalonDock:LayoutAnchorSide>
                            <avalonDock:LayoutAnchorGroup>
                                <avalonDock:LayoutAnchorable x:Name="StatusAnchorable" ContentId="Status" Title="Status" CanClose="False" CanHide="True" AutoHideHeight="100">
                                    <TextBox Name="Status" TextWrapping="Wrap" Padding="3" ScrollViewer.VerticalScrollBarVisibility="Auto" />
                                </avalonDock:LayoutAnchorable>
                            </avalonDock:LayoutAnchorGroup>
                        </avalonDock:LayoutAnchorSide>
                    </avalonDock:LayoutRoot.BottomSide>

                </avalonDock:LayoutRoot>
            </avalonDock:DockingManager.Layout>
        </avalonDock:DockingManager>

    </Grid>

    <Window.InputBindings>
        <KeyBinding Command="{Binding FindCommand}" Gesture="Ctrl+F" />
        <KeyBinding Command="{Binding TeleportCommand}" Gesture="Ctrl+T" />
        <KeyBinding Command="{Binding HistoryCommand}" Gesture="Backspace" />
        <KeyBinding Command="{Binding ForwardCommand}" Gesture="Alt+Right" />
        <KeyBinding Command="{Binding DebugCommand}" Gesture="Ctrl+D" />
    </Window.InputBindings>
</Window>
